# Runtime mode label (default: dev).
MINIFLOW_ENV=dev
MINIFLOW_CONFIG=configs/baseline.yml
MINIFLOW_REQUEST_TIMEOUT_SECONDS=120

# Max upload payload for /s2s in bytes (10 MiB default).
MINIFLOW_MAX_AUDIO_UPLOAD_BYTES=10485760
RELEASE_ID=dev
